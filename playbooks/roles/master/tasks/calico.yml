---
  - name: Calico Setup
    block:
      - name: Calico Setup | Install
        shell: kubectl apply -f https://docs.projectcalico.org/manifests/calico.yaml
        register: result
        retries: 5
        delay: 5
        until: "result is not failed"