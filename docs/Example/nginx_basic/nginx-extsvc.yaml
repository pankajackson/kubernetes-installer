apiVersion: v1
kind: Service
metadata:
  name: nginxsvc-php
  labels:
    app: nginx-php
spec:
  type: LoadBalancer
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx-php
  type: LoadBalancer
